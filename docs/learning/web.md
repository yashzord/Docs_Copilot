# Web track

One section per topic. Nothing yet. First sections arrive with D1 step 2.

```
(planned: HTTP request and response, FastAPI, async, SSE streaming,
 Next.js App Router, server vs client components, TypeScript strict)
```
