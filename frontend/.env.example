# Copy to frontend/.env.local (gitignored). Read only on the server, never sent to the browser.
# Where the FastAPI backend listens. Use the port you started uvicorn on.
API_URL=http://localhost:8001
